- page_header "Vos conversations"
%p= link_to 'Envoyer un message', new_message_path, class: 'btn btn-lg btn-primary'
%ul.list-group
  = render partial: 'conversations/conversation', collection: @conversations
= paginate @conversations
