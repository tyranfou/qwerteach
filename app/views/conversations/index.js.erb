$('#mailbox_conversations').html("<%= j render partial: 'mailbox_conversations',
                                       locals: {conversations: @conversations,
                                       user: @user}%>");
$('#mailbox_options').html("<%= j render partial: 'mailbox_options',
                                       locals: {conversations: @conversations,
                                       mailbox_type: @mailbox_type}%>");

$('#mailbox_head').html("<%= j render partial: 'mailbox_head',
                                        locals: {mailbox_type: @mailbox_type} %>");
$('#mailbox_navigation').html("<%= j render partial: 'mailbox_navigation',
                                            locals: {mailbox_type: @mailbox_type,
                                            unread_count: @unread_count}%>");

$('.inbox-nav-link').each(function(el) {
  $(this).parent().removeClass('active');
  if($(this).attr('data-boxtype') == '<%= @mailbox_type %>')
  {
    $(this).parent().addClass('active');
  }
});



if('<%= @mailbox_type %>' == 'trash') {
  $('#untrash').show();
  $('#trash').hide();
}
else {
  $('#untrash').hide();
  $('#trash').show();
}

mailbox_links();