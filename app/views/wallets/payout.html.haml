%p
  Montant à transférer :
  = wallet_balance @user.normal_wallet
= form_tag make_payout_path, method: :put do |f|
  .field
    = label_tag :account
    %br/
    = select_tag :account, options_for_select(@user.bank_accounts.collect{|ba| [ba.iban, ba.id]}), :required => true
  .field
    = submit_tag 'Confirmer', :class => 'btn btn-primary btn-mini'
    %br/