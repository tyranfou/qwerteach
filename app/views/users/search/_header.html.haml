#search_header{"data-vide-bg" => "mp4: #{video_path('videos/accueil/bigimage.mp4')} , webm: #{video_path('videos/accueil/bigimage.webm')} , poster: #{image_path('search/search_header.jpg')}", "data-vide-options" => "posterType: jpg, loop: ture, muted: true, position: 50% 50%"}
  %h1
    Trouvez votre #{@topic.nil? ? "prof particulier" : "prof de #{@topic.title.downcase}"}
    %br
    pour un
    %span.text-green
      cours particulier en ligne.
  .mode_emploi
    %a.open_mode_emploi Mode d'emploi
  .search-controls
    = form_tag profs_path, method: :post, id: 'search-form', remote: false do
      - if @topic_title.nil?
        %span#search-topic.text-appear Tous les profs
        %span.caret
      - else
        Profs de
        %span#search-topic.text-appear #{@topic_title}
        %span.caret
      = hidden_field_tag("topic", params[:topic], id: 'search-topic-field')
      triés par
      %span#search-sorting.text-appear #{params[:search_sorting].nil? ? "pertinence": search_sorting_name}
      %span.caret
      = hidden_field_tag("search_sorting", params[:search_sorting].nil? ? "qwerteach_score": params[:search_sorting].to_s, id: 'search-sorting-field')
      = hidden_field_tag('filter', params[:filter], id:'search-filter')