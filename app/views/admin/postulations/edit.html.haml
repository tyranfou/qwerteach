%body
  - content_for(:title) { "Edit #{page.page_title}" }
  %header.header
    %h1.header__heading= "Edit postulation of #{page.resource.teacher.firstname} #{page.resource.teacher.lastname}"
    .header__actions
      = link_to(                      |
          "Show #{page.page_title}",  |
          [namespace, page.resource], |
          class: "button",            |
        )                             |
  = render "form", page: page
